<!-- Directory.Build.props for .NET best practices (awesome copilot)
     Add analyzers, code quality, and documentation settings here. -->
<Project>
    <PropertyGroup>
        <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
        <Nullable>enable</Nullable>
        <LangVersion>latest</LangVersion>
        <GenerateDocumentationFile>true</GenerateDocumentationFile>
    </PropertyGroup>
    <ItemGroup>
        <!-- Microsoft recommended analyzers - versions managed in Directory.Packages.props -->
        <PackageReference Include="Microsoft.CodeAnalysis.NetAnalyzers" PrivateAssets="all"/>
        <!-- Pin compiler host to Roslyn 5.3.x across the solution to avoid analyzer/compiler version drift. -->
        <PackageReference Include="Microsoft.Net.Compilers.Toolset" PrivateAssets="all"/>
        <!--        <PackageReference Include="StyleCop.Analyzers" PrivateAssets="all"/>-->
    </ItemGroup>
    <ItemGroup>
        <!-- StyleCop configuration -->
        <AdditionalFiles Include="$(MSBuildThisFileDirectory)stylecop.json" Link="stylecop.json"/>
    </ItemGroup>
</Project>

